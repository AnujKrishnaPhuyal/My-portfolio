import { useState } from 'react'; import { ChevronDown, ChevronUp } from 'lucide-react';
import { projects } from '../data/projects'; import type { ProjectCategory } from '../types';
import { SectionTitle } from '../components/SectionTitle'; import { ProjectCard } from '../components/ProjectCard'; import { Reveal } from '../components/Reveal';
const filters: ('All'|ProjectCategory)[]=['All','AI/ML','Generative AI','Research','Software Engineering'];
export function Projects(){const [filter,setFilter]=useState<(typeof filters)[number]>('All');const [more,setMore]=useState(false);const matched=projects.filter(p=>filter==='All'||p.category.includes(filter));const visible=more?matched:matched.slice(0,4);return <section id="projects" className="section"><div className="container"><Reveal><SectionTitle eyebrow="Selected work" title="Projects built around difficult problems" description="Research systems, machine learning workflows, and software engineered for clarity and impact."/></Reveal><div className="filters" role="group" aria-label="Filter projects">{filters.map(f=><button className={filter===f?'active':''} onClick={()=>{setFilter(f);setMore(false)}} key={f}>{f}</button>)}</div><div className="projects-grid">{visible.map(p=><ProjectCard key={p.title} project={p}/>)}</div>{matched.length>4&&<button className="button more-button" onClick={()=>setMore(v=>!v)}>{more?<><ChevronUp/>Show fewer projects</>:<><ChevronDown/>View more projects</>}</button>}</div></section>}
