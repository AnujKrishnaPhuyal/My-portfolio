import { Activity, Binary, BrainCircuit, CheckCircle2, ShieldCheck } from 'lucide-react';
import { SectionTitle } from '../components/SectionTitle'; import { Reveal } from '../components/Reveal'; import { researchInterests } from '../data/skills';
import { researchContent } from '../data/research';
export function Research(){const c=researchContent;return <section id="research" className="section alt"><div className="container"><Reveal><SectionTitle {...c.heading}/></Reveal><div className="research-feature"><Reveal className="research-panel primary"><div className="research-label"><Activity/> {c.featured.label}</div><h3>{c.featured.title}</h3><h4>{c.featured.subtitle}</h4><p>{c.featured.description}</p><ul className="check-list">{c.featured.highlights.map(x=><li key={x}><CheckCircle2/>{x}</li>)}</ul><div className="research-metric"><strong>{c.featured.metric}</strong><span>{c.featured.metricLabel}</span></div></Reveal><Reveal className="research-panel"><div className="research-label"><Binary/> {c.ongoing.label}</div><h3>{c.ongoing.title}</h3><h4>{c.ongoing.subtitle}</h4><p>{c.ongoing.description}</p><ul className="check-list">{c.ongoing.highlights.map(x=><li key={x}><CheckCircle2/>{x}</li>)}</ul><div className="model-row">{c.ongoing.models.map((model,i)=><span key={model}>{i===0?<BrainCircuit/>:<ShieldCheck/>}{model}</span>)}</div></Reveal></div><Reveal><div className="interests"><span className="kicker">Research interests</span><div>{researchInterests.map(i=><span key={i}>{i}</span>)}</div></div></Reveal></div></section>}
