import { ArrowDown, ArrowRight, FlaskConical, MapPin } from 'lucide-react';
import { motion, useReducedMotion } from 'framer-motion';
import { personalInfo } from '../data/personal';
import { heroContent } from '../data/hero';
import { SocialLinks } from '../components/SocialLinks';
export function Hero() { const reduce = useReducedMotion(); const c=heroContent; return <section id="home" className="hero"><div className="hero-grid" aria-hidden="true"/><div className="hero-glow one"/><div className="hero-glow two"/><div className="container hero-content"><motion.div className="hero-intro-top" initial={reduce ? false : { opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .65 }}><div className="availability"><i/> {personalInfo.availability}</div><p className="hello">{c.greeting}</p><h1>{c.firstName} <span>{c.lastName}</span></h1></motion.div><motion.div className="hero-intro-rest" initial={reduce ? false : { opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .65, delay: .1 }}><h2>{personalInfo.title}</h2><p className="hero-copy">{c.description}</p><p className="lab"><FlaskConical size={18}/> {c.researchLine}</p><div className="hero-actions"><a className="button primary" href="#projects">{c.actions.work} <ArrowRight size={17}/></a><a className="text-link" href="#contact">{c.actions.contact}</a></div><div className="hero-bottom"><SocialLinks labeled/><span><MapPin size={16}/>{personalInfo.location}</span></div></motion.div><motion.div className="hero-mark" initial={reduce ? false : {opacity:0,scale:.9}} animate={{opacity:1,scale:1}} transition={{duration:.7,delay:.2}}><div className="mark-ring r1" aria-hidden="true"/><div className="mark-ring r2" aria-hidden="true"/><motion.div className="profile-frame" whileHover={reduce ? undefined : { y: -8, rotate: 2, scale: 1.03 }} whileTap={reduce ? undefined : { scale: .98 }}><img src={personalInfo.profileImage} alt={personalInfo.profileImageAlt}/></motion.div></motion.div></div><a className="scroll-hint" href="#about" aria-label="Scroll to about"><ArrowDown/></a></section>; }
