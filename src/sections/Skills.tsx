import { skillGroups } from '../data/skills'; import { SectionTitle } from '../components/SectionTitle'; import { Reveal } from '../components/Reveal';
export function Skills(){return <section id="skills" className="section alt"><div className="container"><Reveal><SectionTitle eyebrow="Technical toolkit" title="Skills for research and production" description="A cross-functional toolkit spanning model development, LLM systems, software engineering, data, and high-performance computing."/></Reveal><div className="skills-grid">{skillGroups.map((g,i)=><Reveal className={`skill-group ${i<2?'wide':''}`} key={g.title}><span className="skill-number">0{i+1}</span><h3>{g.title}</h3><div className="skill-list">{g.skills.map(s=><span key={s}>{s}</span>)}</div></Reveal>)}</div></div></section>}
