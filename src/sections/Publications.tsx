import { publications } from '../data/publications'; import { PublicationCard } from '../components/PublicationCard'; import { SectionTitle } from '../components/SectionTitle'; import { Reveal } from '../components/Reveal';
export function Publications(){return <section id="publications" className="section"><div className="container"><Reveal><SectionTitle eyebrow="Publications" title="Research & writing" description="Published work and active investigations. Links are shown when configured in the data file."/></Reveal><div className="publication-list">{publications.map(p=><Reveal key={p.title}><PublicationCard publication={p}/></Reveal>)}</div></div></section>}
